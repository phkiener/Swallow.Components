<div class="sw-tabs" data-registered="@RendererInfo.IsInteractive">
    <div class="tab-list" role="tablist" @onkeydown="@HandleTabListInput">
        @foreach (var tab in tabManager.Tabs)
        {
            <button type="button"
                    class="sw text"
                    id="@tab.Id-handle"
                    role="tab"
                    tabindex="@(tab.Selected ? "0" : "-1")"
                    aria-selected="@(tab.Selected ? "true" : "false")"
                    aria-controls="@tab.Id-panel"
                    @onclick="@(() => HandleTabClick(tab))"
                    @onkeydown="@(evt => HandleTabInput(evt, tab))">
                @tab.Title
            </button>
        }
    </div>

    <div class="tab-panels">
        @foreach (var tab in tabManager.Tabs)
        {
            <div id="@tab.Id-panel"
                 role="tabpanel"
                 tabindex="0"
                 hidden="@(!tab.Selected)"
                 aria-labelledby="@tab.Id-handle">
                @tab.Content
            </div>
        }
    </div>

    <CascadingValue Value="tabManager" IsFixed>
        @ChildContent
    </CascadingValue>
</div>
